import { useState, useEffect } from "react";
import { Outlet } from "react-router-dom";

const ConnectWallet = () => {
  const [isLoading, setIsLoading] = useState(false);

  return <div>welcome conenct wallet</div>;
};

export default ConnectWallet;
